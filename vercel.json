{
  "buildCommand": "sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/g' prisma/schema.prisma && prisma generate && (prisma db push --accept-data-loss 2>/dev/null || echo 'Warning: Database push skipped - DATABASE_URL may not be configured') && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["bom1"]
}
